fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0   # lets `uvicorn --env-file .env` load backend/.env
httpx>=0.27          # outbound client for /convert: follow short-link redirects, scrape, geocode
