# Backend folder store (FastAPI, M2)
VITE_API_BASE=http://localhost:3001

# Editor framed in the iframe.
#   mock (M1–M4):  /fake-editor.html      (served from ../dev via publicDir)
#   real (M8):     http://localhost:5180/app?embedded=1
VITE_EDITOR_URL=/fake-editor.html

# Poll interval for collaboration (ms)
VITE_POLL_MS=2000
